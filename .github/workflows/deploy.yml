name: Deploy Backend to Render

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Trigger Render deployment
        run: |
          echo "Deployment triggered. Configure webhook in Render dashboard."
          # Render auto-deploys on push to main once connected
